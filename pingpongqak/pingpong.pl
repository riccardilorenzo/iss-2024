%====================================================================================
% pingpong description   
%====================================================================================
dispatch( ping, ping(N) ).
dispatch( pong, pong(N) ).
%====================================================================================
context(ctxpp, "localhost",  "TCP", "8000").
 qactor( pinger, ctxpp, "it.unibo.pinger.Pinger").
 static(pinger).
